sources += configure_file(
    input : 'Build.vala.in',
    output : 'Build.vala',
    configuration : config
)

sources += files(
    'Application.vala'
)

subdir('API')
subdir('Dialogs')
subdir('Services')
subdir('Utils')
subdir('Views')
subdir('Widgets')
