sources += files(
    'FilterEdit.vala',
    'ListEdit.vala',
    'MainWindow.vala',
    'NewAccount.vala',
    'Preferences.vala',
    'ProfileEdit.vala',
    'Report.vala',
    'Saveable.vala',
)

if devmode
  sources += files ('DevConsole.vala')
endif

subdir('Composer')
