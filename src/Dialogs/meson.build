sources += files(
    'ListEdit.vala',
    'MainWindow.vala',
    'NewAccount.vala',
    'Preferences.vala',
    'ProfileEdit.vala',
    'Saveable.vala',
)

if devmode
  sources += files ('DevConsole.vala')
endif

subdir('Composer')
